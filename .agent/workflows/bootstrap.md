# /bootstrap

1. 請先進入 `docs/` 搜尋相關規範，完成任務啟動，不寫程式。

輸出格式：

1. Goal（任務目標，1 句）
2. Paths（目標檔案）
3. Constraints（限制條件）
4. Acceptance Criteria（驗收標準）
5. Non-goals（非目標）

檢查點：

- 已執行 `find docs/` 並列出受影響的既有文檔。
- 不帶入未提供假設。
- 未完成 bootstrap 前，不進入規劃或實作。
